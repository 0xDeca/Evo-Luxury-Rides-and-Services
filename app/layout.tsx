import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Eko Luxury Rides and Service Apartments',
  description: 'Premium luxury car rentals and service apartments across Nigeria. Experience luxury rides and comfortable stays in Lagos, Abuja, and major Nigerian cities.',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}